.goods-list-wrap {
  display: flex;
  flex-flow: row wrap;
  justify-content: space-between;
  padding: 0;
  background: transparent;
}

.goods-card-inside {
  width: calc((100% - 16rpx) / 2);
  box-sizing: border-box;
}
